Create a service account named backend-team. Print out the token for the service account in yaml.

Create a pod named backend that uses the the image pihole/pihole and the identity backend-team 
for running processes.

Shell to the pod and print out the token for the service account.

